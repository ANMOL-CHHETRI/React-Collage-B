# Server-side Firebase Functions Environment Variables
# In production, Firebase automatically provides credentials via Application Default Credentials.
# For local emulator suite testing:
FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
FIRESTORE_EMULATOR_HOST=localhost:8080
FIREBASE_STORAGE_EMULATOR_HOST=localhost:9199
